name: Check Third Party Notices File

on: push

jobs:
  call_notices_check:
    uses:  yext/answers-headless/.github/workflows/third_party_notices_check_reuse.yml@dev/test-reusable-workflows
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
